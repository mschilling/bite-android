<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="TextViewCustom">
        <attr name="customFont" format="string"/>
    </declare-styleable>

    <declare-styleable name="RelativeLayoutCircle">
        <attr name="customCircleColor" format="reference|color"/>
        <attr name="customStrokeWidth" format="reference|dimension"/>
    </declare-styleable>

    <declare-styleable name="RelativeLayoutStatsTitle">
        <attr name="customStatsTitleColor" format="reference|color"/>
    </declare-styleable>

    <!-- General CardView -->
    <attr name="cardViewBackground" format="reference|color"/>

    <!-- FAB -->
    <attr name="colorFabRipple" format="reference|color"/>

    <!-- Card stipe -->
    <attr name="colorCardStripe" format="reference|color"/>

    <!-- Empty Bites -->
    <attr name="colorEmptyBites" format="reference|color"/>

</resources>